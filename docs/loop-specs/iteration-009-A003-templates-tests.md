# [A-003] Templates 组件测试补充

## 问题

`apps/dashboard/src/components/Templates.tsx` 当前覆盖率 0%，共 525 行代码。该组件是测试模板管理页面，包含：

- 模板列表展示
- 模板创建/编辑/删除
- 模板分类管理
- 模板搜索
- 模板导入/应用

## 根因

同 A-001，Dashboard 组件测试历史遗留。

## 修复方案

### 测试文件位置
`apps/dashboard/src/__tests__/Templates.test.tsx`

### Mock 策略
```typescript
// Mock API
vi.mock('../lib/api', () => ({
  api: {
    templates: {
      list: vi.fn(),
      get: vi.fn(),
      create: vi.fn(),
      delete: vi.fn(),
    },
  },
}));
```

### 测试用例设计

1. **渲染测试**
   - 渲染模板列表
   - 显示空状态
   - 显示加载中

2. **CRUD 测试**
   - 创建模板
   - 查看模板详情
   - 删除模板（含确认）
   - 验证必填字段

3. **分类筛选测试**
   - 按分类筛选
   - 按关键词搜索
   - 清除筛选

4. **模板应用测试**
   - 应用模板创建任务
   - 模板预览

### 验收标准

- [ ] 测试文件创建并通过
- [ ] 覆盖率 >70%
- [ ] CRUD 和筛选路径覆盖
- [ ] 错误状态处理

## 回归风险

**低** — 纯新增测试文件。

## 预计收益

- 覆盖率贡献：+0.4-0.8%（整体）
- 测试数量：+15-20 个
